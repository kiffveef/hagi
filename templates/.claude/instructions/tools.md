# Preferred Tools

Shell: `grep`→`rg`, `cat`→`bat -p`, `find`→`fd`, `pip`→`uv`

File operations: use Read/Edit/Write/Grep/Glob tools. Never use bash for file reading, writing, searching, or editing.
Bash only for: system commands, package managers, git, build tools.
