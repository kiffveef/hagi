# General Rules

- Run independent operations concurrently
- Use `get_symbols_overview`/`find_symbol` before reading entire files
- Time-sensitive facts (versions, dates, compatibility) → verify via WebSearch or Context7. If unavailable: prefix with "未検証(学習データに基づく)"
- Always check Context7 MCP before using a library
